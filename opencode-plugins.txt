# OpenCode 2.0 plugins: external packages loaded in ~/.config/opencode/opencode.jsonc.
# Local plugins (e.g. plugins/rtk.ts) are discovered automatically.
# Validate with ./scripts/validate.sh.
@plannotator/opencode
